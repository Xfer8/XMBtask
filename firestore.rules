rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // XMBtask v1 is retired. Client access remains closed until the replacement
    // application's server-enforced whitelist rules are ready to deploy.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
