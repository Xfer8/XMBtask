rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // XMBtask v1 is retired. Storage stays closed until replacement rules are
    // designed and deployed with the new application.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
